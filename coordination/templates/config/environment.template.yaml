schema_version: environment_config_v0_1
status: template

environment:
  name: local
  mode: development
  debug: true

runtime:
  host: 127.0.0.1
  port: 8000
  reload: true

logging:
  level: INFO
  format: text

features:
  sandbox_mode: true
  production_write_enabled: false
