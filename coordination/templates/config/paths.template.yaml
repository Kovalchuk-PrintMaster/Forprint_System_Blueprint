schema_version: paths_config_v0_1
status: template

paths:
  project_root: .
  app_dir: app
  config_dir: config
  coordination_dir: coordination
  docs_dir: docs
  examples_dir: examples
  scripts_dir: scripts
  tests_dir: tests
  reports_dir: reports
  data_dir: data
  logs_dir: logs
  tmp_dir: tmp

runtime_paths:
  input_dir: data/input
  output_dir: data/output
  staging_dir: data/staging
  cache_dir: tmp/cache
