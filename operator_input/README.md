# Operator Input

This root contains short-lived, explicitly requested operator or assistant
input for deterministic workflows.

Files under module folders are runtime input, not canonical project truth.

A script must print the exact expected path and schema before asking for input.
