# Blueprint Operator Input

The Blueprint Self Audit expects:

```text
bsa.yaml
```

The file is generated by:

```text
make blueprint-self-audit
```

After completing the generated response:

```text
make blueprint-self-audit-resume
```

`bsa.yaml` is intentionally ignored by Git.
