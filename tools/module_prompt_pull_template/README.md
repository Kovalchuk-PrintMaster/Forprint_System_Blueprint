# ForPrint Module Prompt Pull Template

This directory contains the canonical template for module-side Blueprint outgoing prompt reading.

The current production candidate is still being hardened in forprint_integration_gateway.

Do not blindly copy this template into all modules until Gateway prompt-pull behavior is accepted as stable.

Required module targets:

make blueprint-prompts-list
make blueprint-prompt
make blueprint-prompt-check
