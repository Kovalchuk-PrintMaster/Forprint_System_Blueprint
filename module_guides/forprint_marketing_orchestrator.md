# ForPrint Marketing Orchestrator

> Generated from `machine/*.yaml`. Не редагувати вручну як джерело правди; правки вносити в YAML.

## Module ID

`forprint_marketing_orchestrator`

## Type

`marketing_content_orchestrator`

## Status

`future`

## Role

Майбутній marketing/content orchestration модуль для campaign planning, social channels, AI media providers та marketing analytics.

## Owns

- Немає явно зафіксованих пунктів.

## Consumes

- Немає явно зафіксованих пунктів.

## Provides

- Немає явно зафіксованих пунктів.

## Must not own

- Немає явно зафіксованих пунктів.

## Incoming data flows

- Немає.

## Outgoing data flows

- Немає.

## Consumed contracts

- Немає.

## Provided contracts

- Немає.

## Prompt for module chat

Ти працюєш над модулем `forprint_marketing_orchestrator` у системі ForPrint. Твоя роль, межі відповідальності, вхідні й вихідні дані мають відповідати цьому guide. Якщо поточна реалізація модуля конфліктує з цим описом, не змінюй архітектуру самостійно — сформуй запит до ForPrint System Blueprint через `coordination/incoming_requests/forprint_marketing_orchestrator/new/`.

## Required discipline

1. Не брати у власність чужі data objects.
2. Не вигадувати неузгоджені контракти.
3. Якщо потрібен новий контракт — сформувати incoming request до Blueprint.
4. Після завершеного проміжного кроку запускати тести свого модуля.
5. У майбутньому підтримувати `forprint_module_manifest.yaml`.
